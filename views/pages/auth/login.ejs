<!DOCTYPE html>
<html lang="id" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Inventaris IT</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        :root { --bs-primary-rgb: 13, 110, 253; --bs-link-color-rgb: var(--bs-primary-rgb); }
        [data-bs-theme="dark"] { --bs-body-bg: #16181c; --bs-body-color: #adb5bd; --bs-border-color: #3e444a; --bs-emphasis-color: #f8f9fa; --bs-secondary-bg: #212529; --bs-tertiary-bg: #2b3035; --bs-link-color-rgb: 122, 181, 255; }
        body { background-color: var(--bs-body-bg); color: var(--bs-body-color); transition: background-color 0.3s ease, color 0.3s ease; font-size: 0.9rem;}
        .login-container { min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 15px;}
        .theme-toggle-corner { position: fixed; top: 1rem; right: 1rem; cursor: pointer; font-size: 1.1rem; z-index: 10; color: var(--bs-secondary-text-emphasis); padding: 0.3rem 0.6rem; border-radius: 50%;}
        .theme-toggle-corner:hover { color: var(--bs-primary); background-color: var(--bs-tertiary-bg); }
        [data-bs-theme="dark"] .theme-toggle-corner:hover { color: var(--bs-primary-text-emphasis); background-color: var(--bs-secondary-bg);}
         .card { border: 1px solid var(--bs-border-color-translucent); background-color: var(--bs-card-bg, #fff); border-radius: 0.5rem; /* Radius lebih besar */ }
         .card-header { background-color: transparent; border-bottom: 1px solid var(--bs-border-color-translucent); padding-top: 1.5rem; padding-bottom: 1rem;}
         .form-label.small { font-size: 0.8rem; margin-bottom: 0.2rem; font-weight: 500; }
         .input-group-text { font-size: 0.8rem; background-color: var(--bs-tertiary-bg); border-color: var(--bs-border-color); color: var(--bs-secondary-text-emphasis); }
         [data-bs-theme="light"] .input-group-text { background-color: #e9ecef; border-color: #ced4da; color: #495057;}
         .form-control-sm { font-size: 0.85rem;}
         .btn-sm { font-size: 0.8rem;}
    </style>
    <script> (function() { const theme = localStorage.getItem('theme') || 'light'; document.documentElement.setAttribute('data-bs-theme', theme); })(); </script>
</head>
<body>
    <span id="theme-toggler" class="theme-toggle-corner" title="Ganti Tema"> <i class="bi bi-sun-fill"></i> </span>
    <div class="container login-container">
        <div class="col-md-6 col-lg-4">
             <div class="text-center mb-4">
                 <i class="bi bi-box-seam-fill text-primary" style="font-size: 3.5rem;"></i>
                <h4 class="fw-semibold text-primary mt-2">Inventaris IT</h4>
                <p class="text-muted small">Silakan login untuk melanjutkan</p>
             </div>
            <div class="card shadow-lg">
                <div class="card-header text-center">
                    <h6 class="mb-0 fw-semibold">Login Administrator</h6>
                </div>
                <div class="card-body p-4">
                    <% if (typeof error !== 'undefined' && error) { %>
                        <div class="alert alert-danger d-flex align-items-center py-2 small" role="alert"> <i class="bi bi-exclamation-triangle-fill me-2"></i><div><%= error %></div> </div>
                    <% } %>
                    <form action="/login" method="POST">
                        <div class="mb-3">
                            <label for="email" class="form-label small">Alamat Email</label>
                             <div class="input-group input-group-sm"> <span class="input-group-text"><i class="bi bi-envelope-fill"></i></span> <input type="email" name="email" id="email" class="form-control" required placeholder="email@contoh.com"> </div>
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label small">Password</label>
                             <div class="input-group input-group-sm"> <span class="input-group-text"><i class="bi bi-lock-fill"></i></span> <input type="password" name="password" id="password" class="form-control" required placeholder="********"> </div>
                        </div>
                        <div class="d-grid mt-4">
                            <button type="submit" class="btn btn-primary btn-sm"><i class="bi bi-box-arrow-in-right me-1"></i> Login</button>
                        </div>
                    </form>
                    <div class="text-center mt-3 border-top pt-3">
                        <small class="text-muted">Belum punya akun? <a href="/register" class="link-primary fw-medium">Daftar</a></small>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script> const themeToggler = document.getElementById('theme-toggler'); const currentTheme = localStorage.getItem('theme') || 'light'; const htmlElement = document.documentElement; const sunIcon = '<i class="bi bi-sun-fill"></i>'; const moonIcon = '<i class="bi bi-moon-stars-fill"></i>'; if (currentTheme === 'dark') { themeToggler.innerHTML = moonIcon; } else { themeToggler.innerHTML = sunIcon; } themeToggler.addEventListener('click', () => { if (htmlElement.getAttribute('data-bs-theme') === 'dark') { htmlElement.setAttribute('data-bs-theme', 'light'); localStorage.setItem('theme', 'light'); themeToggler.innerHTML = sunIcon; } else { htmlElement.setAttribute('data-bs-theme', 'dark'); localStorage.setItem('theme', 'dark'); themeToggler.innerHTML = moonIcon; } }); </script>
</body>
</html>