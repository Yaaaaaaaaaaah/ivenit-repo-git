<!DOCTYPE html>
<html lang="id" data-bs-theme="light">
<head>
     <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrasi Pengguna Baru - Inventaris IT</title> <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
         :root { --bs-primary-rgb: 13, 110, 253; --bs-link-color-rgb: var(--bs-primary-rgb); }
        [data-bs-theme="dark"] { --bs-body-bg: #16181c; --bs-body-color: #adb5bd; --bs-border-color: #3e444a; --bs-emphasis-color: #f8f9fa; --bs-secondary-bg: #212529; --bs-tertiary-bg: #2b3035; --bs-link-color-rgb: 122, 181, 255; --bs-card-bg: #212529; --bs-border-color-translucent: rgba(255, 255, 255, 0.1); }
         [data-bs-theme="light"] { --bs-body-bg: #f4f7fc; --bs-card-bg: #ffffff; --bs-border-color-translucent: rgba(0, 0, 0, 0.075); }
        body { background-color: var(--bs-body-bg); color: var(--bs-body-color); transition: background-color 0.3s ease, color 0.3s ease; font-size: 0.9rem;}
        .register-container { min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 15px;}
        .theme-toggle-corner { position: fixed; top: 1rem; right: 1rem; cursor: pointer; font-size: 1.1rem; z-index: 10; color: var(--bs-secondary-text-emphasis); padding: 0.3rem 0.6rem; border-radius: 50%;}
        .theme-toggle-corner:hover { color: var(--bs-primary); background-color: var(--bs-tertiary-bg); }
        [data-bs-theme="dark"] .theme-toggle-corner:hover { color: var(--bs-primary-text-emphasis); background-color: var(--bs-secondary-bg);}
        .card { border: 1px solid var(--bs-border-color-translucent); background-color: var(--bs-card-bg); border-radius: 0.5rem; transition: background-color 0.3s ease, border-color 0.3s ease;}
        .card-header { background-color: transparent; border-bottom: 1px solid var(--bs-border-color-translucent); padding-top: 1.5rem; padding-bottom: 1rem; color: var(--bs-emphasis-color);}
         [data-bs-theme="light"] .card-header { color: var(--bs-dark); }
        .form-label.small { font-size: 0.8rem; margin-bottom: 0.2rem; font-weight: 500; }
        .input-group-text { font-size: 0.8rem; background-color: var(--bs-tertiary-bg); border-color: var(--bs-border-color); color: var(--bs-secondary-text-emphasis); transition: background-color 0.3s ease, border-color 0.3s ease;}
        [data-bs-theme="light"] .input-group-text { background-color: #e9ecef; border-color: #ced4da; color: #495057;}
        .form-control-sm { font-size: 0.85rem;} .btn-sm { font-size: 0.8rem;}
        .form-control:focus, .form-select:focus { border-color: rgba(var(--bs-primary-rgb), 0.5); box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.2); }
    </style>
    <script> (function() { const theme = localStorage.getItem('theme') || 'light'; document.documentElement.setAttribute('data-bs-theme', theme); })(); </script>
</head>
<body>
    <span id="theme-toggler" class="theme-toggle-corner" title="Ganti Tema"> <i class="bi bi-sun-fill"></i> </span>
    <div class="container register-container">
        <div class="col-md-6 col-lg-5">
            <div class="text-center mb-4">
                 <i class="bi bi-box-seam-fill text-primary" style="font-size: 3.5rem;"></i>
                <h4 class="fw-semibold text-primary mt-2">Inventaris IT</h4>
                 <p class="text-muted small">Buat akun baru untuk akses</p>
             </div>
            <div class="card shadow-lg">
                <div class="card-header text-center">
                    <h6 class="mb-0 fw-semibold">Registrasi Pengguna</h6>
                </div>
                <div class="card-body p-4">
                     <% if (typeof error !== 'undefined' && error) { %>
                        <div class="alert alert-danger d-flex align-items-center py-2 small" role="alert"> <i class="bi bi-exclamation-triangle-fill me-2"></i><div><%= error %></div> </div>
                    <% } %>
                    <form action="/register" method="POST">
                        <div class="mb-3">
                            <label for="name" class="form-label small">Nama Lengkap <span class="text-danger">*</span></label>
                             <div class="input-group input-group-sm"> <span class="input-group-text"><i class="bi bi-person-badge-fill"></i></span> <input type="text" name="name" id="name" class="form-control" required placeholder="Nama Anda"> </div>
                        </div>
                        <div class="mb-3">
                            <label for="email" class="form-label small">Alamat Email <span class="text-danger">*</span></label>
                            <div class="input-group input-group-sm"> <span class="input-group-text"><i class="bi bi-envelope-fill"></i></span> <input type="email" name="email" id="email" class="form-control" required placeholder="email@contoh.com"> </div>
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label small">Password <span class="text-danger">*</span></label>
                            <div class="input-group input-group-sm"> <span class="input-group-text"><i class="bi bi-lock-fill"></i></span> <input type="password" name="password" id="password" class="form-control" required placeholder="Minimal 6 karakter"> </div>
                            <div class="form-text small text-muted">Password akan dienkripsi.</div>
                        </div>
                        <div class="d-grid mt-4">
                            <button type="submit" class="btn btn-primary btn-sm"><i class="bi bi-person-plus-fill me-1"></i> Register</button>
                        </div>
                    </form>
                     <div class="text-center mt-3 border-top pt-3">
                        <small class="text-muted">Sudah punya akun? <a href="/login" class="link-primary fw-medium">Login</a></small>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script> const themeToggler = document.getElementById('theme-toggler'); const currentTheme = localStorage.getItem('theme') || 'light'; const htmlElement = document.documentElement; const sunIcon = '<i class="bi bi-sun-fill"></i>'; const moonIcon = '<i class="bi bi-moon-stars-fill"></i>'; if (currentTheme === 'dark') { themeToggler.innerHTML = moonIcon; } else { themeToggler.innerHTML = sunIcon; } themeToggler.addEventListener('click', () => { if (htmlElement.getAttribute('data-bs-theme') === 'dark') { htmlElement.setAttribute('data-bs-theme', 'light'); localStorage.setItem('theme', 'light'); themeToggler.innerHTML = sunIcon; } else { htmlElement.setAttribute('data-bs-theme', 'dark'); localStorage.setItem('theme', 'dark'); themeToggler.innerHTML = moonIcon; } }); </script>
</body>
</html>