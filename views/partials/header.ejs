<!DOCTYPE html>
<html lang="id" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Inventaris IT</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        :root {
            --sidebar-width: 250px;
            --topbar-height: 60px;
            --bs-card-cap-bg: var(--bs-tertiary-bg);
            --bs-border-color-translucent: rgba(0, 0, 0, 0.075);
        }
        body { background-color: var(--bs-light-bg-subtle); }
        .sidebar { width: var(--sidebar-width); height: 100vh; position: fixed; top: 0; left: 0; z-index: 1030; background-color: #212529; color: #adb5bd; transition: margin-left 0.3s ease; }
        .sidebar-header { height: var(--topbar-height); display: flex; align-items: center; padding: 0 1.25rem; border-bottom: 1px solid rgba(255, 255, 255, 0.1); }
        .sidebar-body { overflow-y: auto; flex-grow: 1; }
        .sidebar-footer { padding: 1rem 1.25rem; border-top: 1px solid rgba(255, 255, 255, 0.1); }
        .sidebar .nav-link { color: #adb5bd; padding: 0.75rem 1.25rem; font-weight: 500; }
        .sidebar .nav-link:hover { color: #fff; background-color: rgba(255, 255, 255, 0.05); }
        .sidebar .nav-link.active { color: #fff; background-color: var(--bs-primary); }
        .sidebar .dropdown-toggle.active { color: #fff; }
        .sidebar .nav-link .bi-dot { font-size: 1.2rem; line-height: 0; vertical-align: middle; }
        .page-wrapper { margin-left: var(--sidebar-width); width: calc(100% - var(--sidebar-width)); min-height: 100vh; display: flex; flex-direction: column; transition: margin-left 0.3s ease; }
        .top-navbar { height: var(--topbar-height); position: sticky; top: 0; z-index: 1020; background-color: var(--bs-body-bg); box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); padding: 0 1.5rem; display: flex; align-items: center; justify-content: space-between; }
        .main-content { flex-grow: 1; padding: 1.5rem; }
        .theme-toggle { cursor: pointer; }
        .text-xs { font-size: 0.75rem; }
        .text-gray-300 { color: #dee2e6; }
        .border-start.border-4 { border-left-width: 4px !important; }
        .nav-tabs-responsive .nav-link { color: var(--bs-secondary-color); }
        .nav-tabs-responsive .nav-link.active { color: var(--bs-primary); font-weight: 600; }
    </style>
    <script> (function() { const theme = localStorage.getItem('theme') || 'light'; document.documentElement.setAttribute('data-bs-theme', theme); })(); </script>
</head>
<body>
    <%- include('sidebar') %>
    <div class="page-wrapper">
        <nav class="top-navbar">
            <div>
                <nav aria-label="breadcrumb" style="--bs-breadcrumb-divider: '>';">
                  <ol class="breadcrumb mb-0">
                    <li class="breadcrumb-item"><a href="/" class="text-decoration-none"><i class="bi bi-house-door-fill"></i></a></li>
                    <li class="breadcrumb-item active" aria-current="page"><%= title %></li>
                  </ol>
                </nav>
            </div>
            <div class="d-flex align-items-center">
                 <span id="live-clock" class="navbar-text me-3 small d-none d-sm-inline" title="Waktu Server (Lokal)">
                    Loading...
                 </span>
                 <span id="theme-toggler" class="theme-toggle me-3 fs-5" title="Ganti Tema"> <i class="bi bi-sun-fill"></i> </span>
                 <% if (session && session.userId) { %>
                    <div class="dropdown">
                        <a href="#" class="nav-link dropdown-toggle d-flex align-items-center" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle fs-4 me-2"></i>
                            <span class="d-none d-sm-inline fw-medium"><%= session.userName %></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
                            <li><a class="dropdown-item" href="/logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
                        </ul>
                    </div>
                 <% } %>
            </div>
        </nav>
        <main class="main-content">